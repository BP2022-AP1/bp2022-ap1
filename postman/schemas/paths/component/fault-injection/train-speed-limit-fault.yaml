# /component/fault-injecton/train-speed-limit-fault
get:
    summary: Get ids of train-speed-limit-fault configurations connected to a user.
    operationId: getTrainSpeedLimitFaultConfigurationIds
    parameters:
        - in: query
          name: simulationId
          schema:
              type: string
              format: uuid
          description: Specify id of simulation if you only want to get the train-speed-limit-fault configuration of a single simulation
    responses:
        "200":
            description: Successfull operation
            content:
                application/json:
                    schema:
                        type: array
                        items:
                            type: string
                            format: uuid
        "401":
            description: API token missing
        "404":
            description: Simulation not found
put:
    summary: Insert a new train-speed-limit-fault component. Returns the id of the configuration object.
    operationId: createTrainSpeedLimitFaultConfiguration
    requestBody:
        content:
            application/json:
                schema:
                    $ref: "../../../schemas/FaultInjector.yaml#/components/schema/TrainSpeedLimitFaultConfiguration"
    responses:
        "201":
            description: Successfully created new train-speed-limit-fault configuration object
            content:
                application/json:
                    schema:
                        type: object
                        properties:
                            id:
                                type: string
                                format: uuid
        "401":
            description: API token missing
        "409":
            description: Train-speed-limit-fault configuration exists with this id already