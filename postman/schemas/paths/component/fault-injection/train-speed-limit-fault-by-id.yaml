# /component/fault-injection/train-speed-limit-fault/{id}
get:
    summary: Get all information about a train-speed-limit-fault configuration.
    operationId: getTrainSpeedLimitFaultConfiguration
    parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
    responses:
        "200":
            description: Successfull operation
            content:
                application/json:
                    schema:
                        $ref: "../../../schemas/FaultInjector.yaml#/components/schema/TrainSpeedLimitFaultConfiguration"
        "401":
            description: API token missing
        "404":
            description: Id not found
delete:
    summary: Delete a train-speed-limit-fault configuration.
    operationId: deleteTrainSpeedLimitFaultConfiguration
    parameters:
        - in: path
          name: id
          required: true
          schema:
              type: string
              format: uuid
    responses:
        "204":
            description: Deleted train-speed-limit-fault configuration
        "401":
            description: API token missing
        "404":
            description: Id not found
        
patch:
    summary: Edit the a train-speed-limit-fault configuration.
    operationId: updateTrainSpeedLimitFaultConfiguration
    parameters:
        - in: path
          name: id
          required: true
          schema:
                type: string
                format: uuid
    requestBody:
        content:
            application/json:
                schema:
                    $ref: "../../../schemas/FaultInjector.yaml#/components/schema/TrainSpeedLimitFaultConfiguration"
    responses:
        "204":
            description: Updated train-speed-limit-fault configuration
        "400":
            description: Update not possible, you can't update the id
        "401":
            description: API token missing
        "404":
            description: Id not found