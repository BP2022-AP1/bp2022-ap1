# /component/fault-injection/train-cancelled-fault/{id}
get:
    summary: Get all information about a train-cancelled-fault configuration.
    operationId: getTrainCancelledFaultConfiguration
    parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
    responses:
        "200":
            description: Successful operation
            content:
                application/json:
                    schema:
                        $ref: "../../../schemas/FaultInjector.yaml#/components/schema/TrainCancelledFaultConfiguration"
        "401":
            description: API token missing
        "404":
            description: Id not found
delete:
    summary: Delete a train-cancelled-fault configuration.
    operationId: deleteTrainCancelledFaultConfiguration
    parameters:
        - in: path
          name: id
          required: true
          schema:
              type: string
              format: uuid
    responses:
        "204":
            description: Deleted train-cancelled-fault configuration
        "401":
            description: API token missing
        "404":
            description: Id not found
        
patch:
    summary: Edit a train-cancelled-fault configuration.
    operationId: updateTrainCancelledFaultConfiguration
    parameters:
        - in: path
          name: id
          required: true
          schema:
                type: string
                format: uuid
    requestBody:
        content:
            application/json:
                schema:
                    $ref: "../../../schemas/FaultInjector.yaml#/components/schema/TrainCancelledFaultConfiguration"
    responses:
        "204":
            description: Updated train-cancelled-fault configuration
        "401":
            description: API token missing
        "404":
            description: Id not found