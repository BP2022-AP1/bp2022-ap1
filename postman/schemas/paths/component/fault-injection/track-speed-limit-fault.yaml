# /component/fault-injecton/track-speed-limit-fault
get:
    summary: Get ids of track-speed-limit-fault configurations connected to a user.
    operationId: getTrackSpeedLimitFaultConfigurationIds
    parameters:
        - in: query
          name: simulationId
          schema:
              type: string
              format: uuid
          description: Specify id of simulation if you only want to get the track-speed-limit-fault configuration of a single simulation
    responses:
        "200":
            description: Successful operation
            content:
                application/json:
                    schema:
                        type: array
                        items:
                            type: string
                            format: uuid
        "401":
            description: API token missing
        "404":
            description: Simulation not found
put:
    summary: Insert a new track-speed-limit-fault component. Returns the id of the configuration object.
    operationId: createTrackSpeedLimitFaultConfiguration
    requestBody:
        content:
            application/json:
                schema:
                    $ref: "../../../schemas/FaultInjector.yaml#/components/schema/TrackSpeedLimitFaultConfiguration"
    responses:
        "201":
            description: Successfully created new track-speed-limit-fault configuration object
            content:
                application/json:
                    schema:
                        type: object
                        properties:
                            id:
                                type: string
                                format: uuid
        "401":
            description: API token missing
        "409":
            description: Train-speed-limit-fault configuration exists with this id already